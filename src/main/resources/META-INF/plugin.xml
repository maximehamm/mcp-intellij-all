<?xml version="1.0" encoding="UTF-8"?>
<idea-plugin>
    <id>io.nimbly.mcp-companion</id>
    <name>MCP Server AI Companion</name>
    <vendor email="Maxime.HAMM@nimbly-consulting.com" url="https://github.com/maximehamm/mcp-intellij-all">Maxime HAMM</vendor>

    <description><![CDATA[

        <p>
        <b>More than 50 tools — give your AI assistant full access to your IDE: compilation errors, test results, debug variables, run configurations, inspections, VCS changes, database schema and data, and much more.</b>
        </p>

        <h4>Editor &amp; Navigation</h4>
        <ul>
            <li><b>get_open_editors</b> — Open files, focused editor with caret position, and current text selection.</li>
            <li><b>get_file_problems</b> — IDE-detected errors, warnings, and infos for a file or all open editors.</li>
            <li><b>get_quick_fixes</b> — Quick fix suggestions at a specific file:line:column. Use after get_file_problems to get actionable fixes for a reported problem.</li>
            <li><b>navigate_to</b> — Opens a file and places the cursor at a given line and column.</li>
            <li><b>select_text</b> — Opens a file and selects a text range (ready to copy with Cmd+C).</li>
            <li><b>highlight_text</b> — Highlights multiple zones in a file using the IDE's search-result color (theme-aware). Press Escape to clear.</li>
            <li><b>clear_highlights</b> — Removes all highlights added by <code>highlight_text</code>.</li>
        </ul>

        <h4>Build &amp; Tests</h4>
        <ul>
            <li><b>get_build_output</b> — Build tool window: structured error/warning tree with file names and line numbers, plus raw console text.</li>
            <li><b>get_console_output</b> — Console output from both the Run and Debug tool windows. All tabs from each window, with the active window and active tab clearly indicated.</li>
            <li><b>get_services_output</b> — All sessions from the Services tool window, grouped by node name: SQL output log, result grids with column names, and which session/tab is currently active.</li>
            <li><b>get_test_results</b> — Last test run results: passed/failed/ignored status, duration, and failure messages with stack traces.</li>
        </ul>

        <h4>Debug</h4>
        <ul>
            <li><b>debug_run_configuration</b> — Launches a run configuration in debug mode.</li>
            <li><b>get_debug_variables</b> — Local variables and their values from the current debugger stack frame (available when paused at a breakpoint).</li>
            <li><b>get_breakpoints</b> — Lists all line breakpoints with file, line number, enabled state, and condition expression.</li>
            <li><b>add_conditional_breakpoint</b> — Adds a breakpoint with an optional condition expression.</li>
            <li><b>set_breakpoint_condition</b> — Sets or removes a condition on an existing breakpoint.</li>
            <li><b>mute_breakpoints</b> — Mutes or unmutes all breakpoints in the active debug session.</li>
        </ul>

        <h4>Diagnostic &amp; Processes</h4>
        <ul>
            <li><b>get_ide_snapshot</b> — Lightweight snapshot of what the developer is currently doing: active file + caret, selection, open files, active runs/debug, indexing, background processes. Designed for frequent polling (e.g. a Claude Code hook to keep the AI aware of the current context).</li>
            <li><b>get_ide_settings</b> — Read IntelliJ IDE settings: Gradle build delegation, SDK, compiler, encoding. Pass a keyword (<code>search</code>) to filter, a <code>key</code> for direct lookup, or a <code>prefix</code> (with optional <code>depth</code>) to retrieve all settings in a subtree.</li>
            <li><b>get_intellij_diagnostic</b> — One-call diagnostic: indexing status, active notifications, running/paused processes, and <code>idea.log</code> WARN/ERROR tail. Use this first when something is not working as expected.</li>
            <li><b>get_running_processes</b> — Lists all active and paused background processes (indexing, Gradle syncs, builds, etc.).</li>
            <li><b>manage_process</b> — Pauses, resumes, or cancels a background process by title.</li>
        </ul>

        <h4>Database</h4>
        <p><em>Requires the Database Tools and SQL plugin (IntelliJ IDEA Ultimate).</em></p>
        <ul>
            <li><b>list_database_sources</b> — Lists all data sources configured in the Database tool window (name, URL, driver, user, DBMS type).</li>
            <li><b>get_database_schema</b> — Returns the schema tree already introspected by IntelliJ: namespaces → tables/views with keys, foreign keys, indexes, and optionally columns.</li>
            <li><b>execute_database_query</b> — Executes a SQL query on a configured data source and returns the results as JSON. ⚠ Disabled by default — can execute UPDATE, DELETE, DROP.</li>
        </ul>

        <h4>VCS</h4>
        <ul>
            <li><b>get_vcs_changes</b> — All locally modified, added, deleted, and moved files (any VCS). Pass <code>includeDiff=true</code> to include a unified diff per file.</li>
            <li><b>get_vcs_branch</b> — Current branch and all local/remote branches for each Git repository.</li>
            <li><b>get_vcs_log</b> — Recent commit history: hash, author, date, subject, and changed files. Filter by file or branch.</li>
            <li><b>get_vcs_blame</b> — Line-by-line blame annotation: who last modified each line, when, and in which commit (any VCS).</li>
            <li><b>get_local_history</b> — IntelliJ Local History for a file or directory: revision list with timestamps, labels (e.g. "Before: Rename"), and optional diffs (unified diff for files, added/modified/deleted paths for directories). Without a path, returns recent project-wide change events.</li>
        </ul>

        <h4>General</h4>
        <ul>
            <li><b>get_mcp_companion_overview</b> — Describes all available MCP Companion tools and how to use them.</li>
            <li><b>get_project_structure</b> — Project SDK, modules, source roots, excluded folders, and module dependencies. All paths relative to project root.</li>
            <li><b>refresh_project</b> — Syncs the project build system (Gradle or Maven). Detects the build system automatically from the project root.</li>
            <li><b>execute_ide_action</b> — Executes any IntelliJ action by ID (e.g. ShowSettings, ReformatCode, GotoFile), or searches for action IDs by keyword.</li>
            <li><b>replace_text_undoable</b> — Replaces text in a file via IntelliJ's document API (supports Cmd+Z undo).</li>
            <li><b>delete_file</b> — Deletes a file from the project (undoable).</li>
        </ul>

        <h3>Example prompts</h3>
        <ul>
            <li><i>"Add a breakpoint at line 18, start the debugger, stop when i == 3, then tell me the value of jj."</i></li>
            <li><i>"Check my build output and fix the compilation errors. Make changes undoable."</i></li>
            <li><i>"Run the tests and tell me which ones failed and why."</i></li>
            <li><i>"Open Main.java and highlight all usages of variable i."</i></li>
            <li><i>"Something is wrong in IntelliJ — run a diagnostic and tell me what you see."</i></li>
        </ul>

        <h3>Settings</h3>
        <p>Each tool can be individually enabled or disabled in
        <b>  Settings → Tools → MCP Server Companion</b>.</p>

        <h3>Source code</h3>
        <p>
            Source code and documentation: <a href="https://github.com/maximehamm/mcp-intellij-all">github.com/maximehamm/mcp-intellij-all</a>
        </p>

    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.mcpServer</depends>

    <extensions defaultExtensionNs="com.intellij">
        <applicationService serviceImplementation="io.nimbly.mcpcompanion.McpCompanionSettings"/>
        <applicationConfigurable
            parentId="tools"
            instance="io.nimbly.mcpcompanion.McpCompanionConfigurable"
            id="io.nimbly.mcpcompanion.configurable"
            displayName="MCP Server Companion"/>
        <postStartupActivity implementation="io.nimbly.mcpcompanion.McpCompanionStartupActivity"/>
        <notificationGroup id="MCP Server Companion" displayType="BALLOON"/>
    </extensions>

    <extensions defaultExtensionNs="com.intellij.mcpServer">
        <mcpToolset implementation="io.nimbly.mcpcompanion.McpCompanionToolset"/>
        <mcpToolset implementation="io.nimbly.mcpcompanion.McpCompanionEditorToolset"/>
        <mcpToolset implementation="io.nimbly.mcpcompanion.McpCompanionBuildToolset"/>
        <mcpToolset implementation="io.nimbly.mcpcompanion.McpCompanionDebugToolset"/>
        <mcpToolset implementation="io.nimbly.mcpcompanion.McpCompanionDiagnosticToolset"/>
        <mcpToolset implementation="io.nimbly.mcpcompanion.McpCompanionCodeAnalysisToolset"/>
        <mcpToolset implementation="io.nimbly.mcpcompanion.McpCompanionDatabaseToolset"/>
        <mcpToolset implementation="io.nimbly.mcpcompanion.McpCompanionVcsToolset"/>
    </extensions>
</idea-plugin>
